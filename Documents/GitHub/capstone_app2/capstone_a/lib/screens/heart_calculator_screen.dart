import 'package:flutter/material.dart';

class HeartCalculatorScreen extends StatelessWidget {
  const HeartCalculatorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Heart Size Matching Calculator'),
      ),
      body: const Center(
        child: Text('Heart size matching calculator functionality coming soon...'),
      ),
    );
  }
} 