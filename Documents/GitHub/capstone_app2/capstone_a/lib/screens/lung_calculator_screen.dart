import 'package:flutter/material.dart';

class LungCalculatorScreen extends StatelessWidget {
  const LungCalculatorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lung Size Matching Calculator'),
      ),
      body: const Center(
        child: Text('Lung size matching calculator functionality coming soon...'),
      ),
    );
  }
} 